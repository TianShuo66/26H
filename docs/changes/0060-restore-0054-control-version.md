# 0060 - 恢复 0054 控制版本

日期：2026-08-01

## 恢复目标

恢复到 [0054-revert-key1-fixed-tilt-sequence.md](0054-revert-key1-fixed-tilt-sequence.md) 对应的控制源文件版本，即提交 `f47e3f1` 的 `Core/Src/main.c`。

## 恢复内容

- 撤销 0055、0056 对 `TASK_NEGATIVE_*` 的所有参数调整。
- 撤销 0057、0058、0059 对中心点微调补偿的所有调整。
- 恢复单一的中心点微调静摩擦补偿：`15 pulse`，触发速度 `0.8 cm/s`。
- 恢复负向近端制动范围 `2.0 cm` 及原有自适应参数。

## 保留内容

- 0054 已恢复的实时级联闭环控制保持不变。
- 历史变更文档不删除，便于继续回退。

## 校验

本次修改后，`Core/Src/main.c` 与提交 `f47e3f1` 的同名文件一致。
