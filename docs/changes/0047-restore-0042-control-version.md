# 恢复 0042 控制版本

- 代码提交：本文件所在提交
- 日期：2026-07-31

## 修改内容

- 按要求将 `Core/Src/main.c` 恢复至 `0042` 对应提交 `31e56e8` 的状态。
- 保留 `-5 cm` 的速度反馈制动。
- 移除之后引入的 `0 cm/-5 cm` 位置无进展递增倾角逻辑，以及回中心高速穿越阻尼限制。
- 所有历史变更文档继续保留，便于再次切换版本。

## 涉及文件

- `Core/Src/main.c`

## 验证

- 使用 Keil ARMCC 完整构建工程，结果见本次提交。
