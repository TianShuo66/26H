# 0090 - 小幅增加 5 到 -5 cm 中段驱动力

## 修改

- 仅将 `+5 -> -5 cm` 中段曲率补偿由 `+12` 提升为 `+16 pulse`。
- 补偿仍只在 `-3.5 ~ -1.5 cm` 且未快速向目标推进时生效。
- `0 -> +5 cm`、中心点微调、`-5 cm` 末段微调与 800 ms 稳定保持不变。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过。
