# 0115 - 略微降低 -5 cm 末段制动

## 修改

- -5 cm 使用的共享末段反制动上限从 `45` 降低到 `40 pulse`。
- 基础制动力和速度增益保持不变，因此仅降低高速接近目标时的最大制动强度。

## 不变项

- 中心点不调用末段反制动，不受此修改影响。
- +5 cm 使用独立末段制动参数，不受此修改影响。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过，`0 Error(s), 0 Warning(s)`。
