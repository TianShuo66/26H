# 0126 - 负向终点制动释放保护

## 修改

- 负向终点制动触发后，增加 `250 ms` 的释放保护期。
- 保护期内禁止静摩擦补偿，只保留位置速度级联控制，等待速度观测稳定。
- 钢球仍朝远离目标方向运动时，同样禁止静摩擦补偿，避免在速度滤波尚未翻转时错误施加 `30 pulse` 推进。

## 保留项

- 仅作用于 KEY1 的负向终点阶段；中心点、KEY3 和 `+5 cm` 控制不变。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过，`0 Error(s), 0 Warning(s)`。
