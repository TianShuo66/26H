# 0145 - KEY3 复用中心维持控制结构

## 修改原因

KEY3 的 `+7 cm` 任务此前使用独立的 `155 pulse` 自适应上限，虽复用了中心任务状态，但控制参数与中心维持不完全一致。

## 修改内容

- 删除 KEY3 独立控制参数。
- `REF=7.0 cm` 与 `REF=0.0 cm` 统一使用中心控制参数：位置速度环、速度门控微调、静摩擦补偿和倾角上限均相同。
- KEY3 与中心任务仅保留目标坐标差异。
- KEY1 任务参数保持不变。

## 验证

- Keil 全量编译：`0 Error(s), 0 Warning(s)`。
