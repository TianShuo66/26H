# 0138 - 修正负向终点反制动方向

## 修改原因

实测中，负向终点回弹阶段输出 `+30~+45 pulse` 后，钢球继续向正方向从约 `-4.2 cm` 滚回 `-2.4 cm`。说明反制动方向与实际机械运动方向相反。

## 修改内容

- 将 KEY1 负向终点回弹阶段的反制动输出从正倾角改为负倾角。
- 保持制动强度范围和其余 KEY1 控制参数不变。

## 验证

- Keil 全量编译：`0 Error(s), 0 Warning(s)`。
