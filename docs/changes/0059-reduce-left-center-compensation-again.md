# 0059 - 再次降低左侧回中心补偿

日期：2026-08-01

## 修改内容

- 钢球从 `-2 -> 0 cm` 回中心时的静摩擦补偿由当前的 `10` 降为 `8 pulse`。

## 保留内容

- 保留工作区已有的中心右侧补偿 `15 pulse`。
- 静摩擦补偿触发速度仍为 `2.0 cm/s`。
- `0 -> +5 cm` 与 `+5 -> -5 cm` 分支未改动。

## 回退

回退此变更可执行：

```powershell
git revert <0059-commit>
```
