# 0142 - 延后 KEY1 负向终点捕获至最后 0.2cm

## 修改原因

实测中，负向捕获在估计位置 `X=-5.3 cm`、目标为 `-6.0 cm` 时已经介入，随后将钢球制动并推回正向，造成大幅回摆，任务在 `5000 ms` 超时。

## 修改内容

- 将 KEY1 负向终点捕获区从最后 `0.5 cm` 缩小到最后 `0.2 cm`。
- 保留现有捕获制动力、负向低速推进和反向后的普通闭环。
- `0 -> +5 cm`、机械零点、视觉坐标与负向目标值均未修改。

## 验证

- Keil 全量编译：`0 Error(s), 0 Warning(s)`。
