# 变更记录

本目录中每个代码修改都对应一份独立 Markdown 文档。文件名按提交顺序递增，文档内记录代码提交、修改原因、涉及文件和验证结果。

## 回退方法

查看变更历史：

```powershell
git log --oneline
```

撤销某项已经推送的修改，使用对应文档中的“代码提交”执行：

```powershell
git revert <代码提交号>
git push
```

`git revert` 会创建一个新的反向提交，不会删除已有历史，适合已经上传到 GitHub 的工程。

后续每一次代码修改会在同一个提交中增加本目录的一份新记录。
