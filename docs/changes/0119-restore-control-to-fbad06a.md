# 0119 - 控制行为恢复至 fbad06a

## 修改

- 控制行为恢复至提交 `fbad06a`。
- KEY1 负向目标恢复为 `-5 cm`，正向阶段在 `+4.0 cm` 切换负向。
- 恢复共享的低强度末段捕获反制动：在距 -5 cm `1 cm` 内触发，最大 `25 pulse`。

## 保留项

- 本地工作区的 `5000 ms` 任务超时、`700 ms` 反向助推及标定注释不纳入此提交。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过，`0 Error(s), 0 Warning(s)`。
