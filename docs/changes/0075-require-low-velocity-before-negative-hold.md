# 0075 - -5 cm 保持前增加速度确认

## 问题

负向目标的保持逻辑此前只判断位置是否处于 `-5 cm +/- 0.7 cm`，未判断钢球速度。钢球尚在回弹时可能提前进入 `TASK_HOLD`，随后电机角度被冻结，钢球无法回到目标位置。

## 修改

进入 `TASK_HOLD` 前，连续 800 ms 的每一帧有效视觉数据必须同时满足：

```text
|位置误差| <= 0.7 cm
|估计速度| <= 2.0 cm/s
```

任意一项不满足都会重新开始计时。仅影响 `-5 cm` 的最终保持判定；机械零点、电机速度和 `0 -> +5 cm` 控制不变。
