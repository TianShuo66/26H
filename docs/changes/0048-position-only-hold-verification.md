# 0048 - 仅按位置判定任务保持

日期：2026-08-01

## 修改内容

- 回中心与 `-5 cm` 任务的保持判定改为：有效视觉位置连续 `800 ms` 落在目标的 `+/-0.7 cm` 范围内，即进入 `TASK_HOLD`。
- 移除保持判定中的速度门限和最少有效帧数门限。
- 保留只由新且被观测器接受的视觉帧推进判定的机制，视觉没有更新时不会错误完成任务。

## 未修改内容

- `+5 cm` 的运动、制动和保持前控制逻辑未改动。
- 电机机械零点、速度、加速度和限位参数未改动。

## 回退

回退此变更可执行：

```powershell
git revert <0048-commit>
```
