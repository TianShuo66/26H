# 0129 - 任务启动时同步电机反馈

## 修改

- 启动 KEY1、KEY2 或 KEY3 闭环任务时，使用刚读取的电机绝对位置相对于机械平衡零点计算当前脉冲。
- 不再在任务启动时把软件电机位置强制写为 `0 pulse`。

## 原因

电机在任务启动前可能仍带有倾角。若软件把它误认为水平，第一条相对运动指令会基于错误位置计算，造成初始倾角继续增大，随后制动时机和力度都会失真。

## 影响

- 不改变机械零点、视觉坐标校准、KEY1 的目标点及既有控制参数。
- 第一轮控制从实际反馈位置平滑衔接到闭环目标。
