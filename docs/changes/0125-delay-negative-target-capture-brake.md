# 0125 - 延后负向目标制动并提高低速推进力

## 修改

- 为负向终点建立独立控制参数，不影响中心点、KEY3 或 `+5 cm` 控制。
- 负向末端微调和反向制动仅在距目标 `0.5 cm` 内启用，避免在距目标约 `1 cm` 时提前反推。
- 球仍在负向目标外且速度不足时，最小推进倾角由 `15 pulse` 提高到 `30 pulse`，用于克服静摩擦。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过，`0 Error(s), 0 Warning(s)`。
