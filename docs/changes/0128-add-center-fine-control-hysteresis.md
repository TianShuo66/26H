# 0128 - 中心末段控制滞回

## 修改

- KEY2 与 KEY3 的末段控制改为带状态的滞回逻辑：误差进入 `<=1.0 cm` 时启用；仅当误差再次超过 `1.5 cm` 时退出。
- 在 `1.0~1.5 cm` 范围内持续使用小倾角末段控制，避免视觉 `0.1 cm` 抖动触发通用 `20 pulse` 静摩擦补偿。
- 负向目标的 `0.5 cm` 末段控制不变。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过，`0 Error(s), 0 Warning(s)`。
