# 0099 - 回退负向巡航速度策略

## 回退内容

- 按要求回退 `aca289d` 的负向巡航速度规则。
- 删除距目标超过 `2 cm` 时强制维持 `-8 cm/s` 的速度参考下限。
- 串口 `VREF` 恢复为原位置外环计算值。

## 恢复后的行为

- 负向控制恢复为提交 `86bf383` 的状态。
- 保留 `-2 ~ -4 cm` 区间的 `+35 pulse` 卡滞补偿及其 `8 cm/s` 速度门限。
- 不修改 `0 -> +5 cm`、中心点、`-6 cm` 目标和最终保持逻辑。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过。
