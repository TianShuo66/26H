# 固定机械零点为 -624

- 代码提交：`b6e36fb`
- 日期：2026-07-31
- 后续状态：已被 `0003` 替代。

## 修改内容

- 新增 `MOTOR_FIXED_ZERO_COUNTS`，初始值为 `-624`。
- 闭环启动和 `A-D` 摆杆辨识测试不再使用启动瞬间的反馈值作为零点。
- 电机脉冲估计统一相对于该固定零点计算。

## 涉及文件

- `Core/Src/main.c`

## 验证

- Keil 构建通过，`0 Error(s), 0 Warning(s)`。

## 说明

实际测试发现驱动器重新上电后位置反馈会归零，因此该数值不适合跨上电使用，随后改为 `0`。
