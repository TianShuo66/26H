# 提高起动倾角与预测停车距离

- 代码提交：本文件所在提交（提交说明：`Expand tilt authority and braking distance`）
- 日期：2026-07-31

## 输入日志结论

- `REF,+5.0` 任务中，钢球最终停在 `+0.7 cm`，但有 `103` 个控制周期处于 `TILT_REF=-68/-70`，原倾角上限已经限制了继续加速。
- `REF,-5.0` 任务中，预测 `STOP_DIST` 多次达到原 `10 cm` 上限，钢球仍越过到 `-11.2 cm`，原停车距离上限过小。

## 修改内容

- 倾角目标上限由 `70` 提高到 `160` 脉冲。
- 静止起动倾角由 `35` 提高到 `100` 脉冲，静态偏置上限由 `25` 提高到 `60` 脉冲，用于克服已观测到的机构静摩擦。
- 预测停车距离上限由 `10 cm` 提高到 `24 cm`，让高速球能在更早的时刻进入反向制动。
- 软件总行程保护保持 `±230` 脉冲，不因本次调参改变。

## 涉及文件

- `Core/Src/main.c`

## 验证

- Keil 构建通过，`0 Error(s), 0 Warning(s)`。
