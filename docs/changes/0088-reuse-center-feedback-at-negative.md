# 0088 - -5 cm 复用中心点实时闭环

## 修改

- `-5 cm` 运行时复用 `0 cm` 的完整控制参数：位置到速度级联、低速自适应推进、末段 `±1.5 cm` 微调和速度反馈反制动。
- 移除负向独立的推进参数、速度扩展制动区和末端捕获制动覆盖，避免与中心点逻辑产生不同的倾角切换。
- `-5 cm` 保留专属的完成条件：进入误差范围且低速持续 `800 ms` 后冻结当前倾角。

## 未修改

- `0 -> +5 cm` 控制参数与代码。
- 电机速度、加速度和机械水平零点。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过。
