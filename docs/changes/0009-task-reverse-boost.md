# 加速任务折返反制动

- 代码提交：本文件所在提交（提交说明：`Boost task reverse braking response`）
- 日期：2026-07-31

## 输入日志结论

- 任务在 `TASK,REVERSE,MS,1134` 正确开始折返，但最终在 `TASK,TIMEOUT,MS,5001` 超时。
- 折返开始时电机为 `PULSE=-67`，必须跨越到正向倾角。
- 旧的高速反制动仅在相邻电机命令方向翻转时触发；本次折返前电机已在同方向回摆，因此未使用高速参数。

## 修改内容

- 在 `TASK,REVERSE` 后启用 `900 ms` 的反制动加速窗口。
- 窗口内使用已有的 `1800 RPM`、加减速参数 `200`。
- 窗口内每个 `40 ms` 控制周期最多追踪 `24` 脉冲；普通控制周期仍限制为 `12` 脉冲。
- 命令实际发生方向翻转时，仍会使用同一高速反制动路径。

## 涉及文件

- `Core/Src/main.c`

## 验证

- Keil 构建通过，`0 Error(s), 0 Warning(s)`。
