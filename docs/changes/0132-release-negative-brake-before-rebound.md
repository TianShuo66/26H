# 0132 - 负向强制制动提前释放

## 修改

- 负向终点的强制捕获制动仅在速度大于 `4.0 cm/s` 时保持。
- 速度降到 `4.0 cm/s` 后，改回级联控制的小倾角减速。

## 原因

提前制动已在距目标约 `1.5 cm` 时介入，但旧的 `1.5 cm/s` 释放阈值使 `-54 pulse` 保持过久，将已到达目标附近的钢球再次推回。

## 影响

- 保留负向首次接近目标时的强制制动。
- 在第一次到达 `-5 cm` 附近前消除大部分速度，再由小倾角结束减速，避免反弹。
