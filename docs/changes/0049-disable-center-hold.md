# 0049 - 取消中心点稳定保持

日期：2026-08-01

## 修改内容

- `TASK_TO_CENTER` 不再参与 `TASK_HOLD` 的 800 ms 稳定判定。
- 回中心任务到达 `0 cm` 后继续运行原有闭环控制，不会冻结当前倾角。
- `TASK_TO_NEGATIVE` 在 `-5 cm` 的 `+/-0.7 cm` 范围内连续 `800 ms` 后进入保持的逻辑保持不变。

## 未修改内容

- `+5 cm` 的运动、制动和保持前控制逻辑未改动。
- 电机机械零点、速度、加速度和限位参数未改动。

## 回退

回退此变更可执行：

```powershell
git revert <0049-commit>
```
