# 0063 - 恢复 c57187d 控制版本

日期：2026-08-01

## 恢复目标

恢复提交 `c57187d`（`Name center and negative control parameters`）对应的 `Core/Src/main.c`。

## 恢复内容

- 恢复回中心 `±1.5 cm` 小倾角微调范围。
- 撤销 0061 的 `±0.7 cm` 微调范围调整。
- 撤销 0062 的左侧 `-2~0 cm` 自适应补偿上限逻辑。

## 保留内容

- 历史变更文档保留，不删除，便于后续按提交回退。
- 本次恢复后，`Core/Src/main.c` 与提交 `c57187d` 的同名文件一致。
