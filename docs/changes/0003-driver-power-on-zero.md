# 驱动器上电零点对齐

- 代码提交：`4f70652`
- 日期：2026-07-31

## 修改内容

- 将 `MOTOR_FIXED_ZERO_COUNTS` 从 `-624` 改为 `0`。
- 明确软件基准：驱动器上电后位置反馈为 `RAW=0`，机构此时处于物理水平零点。

## 涉及文件

- `Core/Src/main.c`

## 验证

- Keil 构建通过，`0 Error(s), 0 Warning(s)`。

## 注意

若驱动器上电时机构不在物理水平位置，必须增加原点开关或人工回零；仅使用 `RAW=0` 无法识别实际机械位置。
