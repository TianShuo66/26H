# 0134 - KEY1 回退到 ae8eb9b

## 修改原因

按测试需求撤销 `3ebbdc0` 对 KEY1 的 `1a6b2be` 控制逻辑恢复，回到提交 `ae8eb9b` 的 KEY1 行为。

## 涉及文件

- `Core/Src/main.c`

## 修改内容

- 恢复 KEY1 的 `0 -> +5 cm -> -5 cm` 目标与 `+4.0 cm` 折返阈值。
- 恢复负向终点的提前捕获、制动释放与回弹后重新起步逻辑。
- 移除 `1a6b2be` 版本新增的负向曲线补偿和独立终点制动函数。

## 验证

- Keil 全量编译：`0 Error(s), 0 Warning(s)`。
