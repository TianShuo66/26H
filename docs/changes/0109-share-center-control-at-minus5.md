# 0109 - -5 cm 复用中心点控制

## 修改

- `TASK_TO_NEGATIVE` 在运行时复用 `TASK_TO_CENTER` 的位置-速度级联、末段微调倾角和静摩擦处理。
- 删除负向专属的曲线补偿、捕获反制动与额外快速步长，避免与中心点控制叠加。
- `-5 cm` 仍保留连续 `800 ms` 的稳定判定，用于 KEY1 任务完成。

## 不变项

- KEY1 负向闭环目标仍为 `-5.0 cm`。
- `+5 cm` 的推进、反转和终端制动逻辑不变。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过，`0 Error(s), 0 Warning(s)`。
