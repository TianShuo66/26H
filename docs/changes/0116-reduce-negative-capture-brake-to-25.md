# 0116 - 将 -5 cm 末段反制动降至 25 pulse

## 现象

- -5 cm 阶段在约 `-3.1 cm` 起触发末段捕获反制动，日志持续显示 `CAPTURE=1` 与 `TILT_REF=-30`。
- 该反制动会使钢球在目标前过早减速。

## 修改

- -5 cm 的末段捕获反制动上限从当前 `30` 降至 `25 pulse`。
- 不修改 `+5 -> -5` 切换时的反向助推时长或 +5 cm 逻辑。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过，`0 Error(s), 0 Warning(s)`。
