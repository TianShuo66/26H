# 0124 - KEY3 目标改为 +7cm，并复用 KEY2 控制流程

## 修改

- KEY3 的闭环目标固定为 `+7.0 cm`（`TASK_KEY3_TARGET_DECI_CM = 70`）。
- 删除 KEY3 专用任务状态；KEY3 与 KEY2 均进入 `TASK_TO_CENTER`。
- KEY3 因此复用 KEY2 的初始化、位置速度级联、末端微调、静摩擦补偿和串口反馈流程，仅目标位置不同。

## 保留项

- KEY1 的 `0 -> +5 -> 负向目标` 任务及其参数不变。
- 当前工作区内的机械零点、视觉坐标和闭环参数本地调整不包含在本次修改中。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过，`0 Error(s), 0 Warning(s)`。
