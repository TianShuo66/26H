# 0093 - 将 KEY1 最终目标改为 -6 cm

## 修改

- KEY1 自动序列调整为 `0 -> +5 -> -6 cm`。
- 负向稳定确认目标同步为 `-6 cm`，到位低速持续 `800 ms` 后保持当前倾角。
- 低速卡滞补偿的实际物理区间保持为 `-2 ~ -4 cm`：目标改变后将对应误差范围调整为 `-4.0 ~ -2.0 cm`。
- `0 -> +5 cm` 的控制代码和参数不变。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过。
