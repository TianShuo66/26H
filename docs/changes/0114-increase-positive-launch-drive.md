# 0114 - 略微提高 +5 cm 正向驱动力

## 修改

- `0 -> +5 cm` 的正向自适应起步倾角从 `35` 提高至 `40 pulse`。
- 仅影响起步和低速推进阶段的最小驱动力。

## 不变项

- `+5 cm` 的位置-速度闭环、末段反制动和反转阈值不变。
- `0 cm` 与 `-5 cm` 的控制参数不变。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过，`0 Error(s), 0 Warning(s)`。
