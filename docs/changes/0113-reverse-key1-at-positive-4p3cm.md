# 0113 - KEY1 在 +4.3 cm 切换负向

## 修改

- KEY1 正向阶段的切换阈值从 `+4.5 cm` 改为 `+4.3 cm`。
- 达到 `X >= +4.3 cm` 后，状态机切换为负向目标。

## 不变项

- `+5 cm` 的闭环控制参数和终端制动参数不变。
- `-5 cm` 的闭环、稳定判定与反制动参数不变。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过，`0 Error(s), 0 Warning(s)`。
