# 0139 - 提前并加强 KEY1 负向反制动

## 修改原因

实测显示负倾角会使钢球继续向正方向回滚；正倾角方向正确，但在速度降到零后才介入，受电机响应延迟影响，钢球已回滚到目标区外。

## 修改内容

- 在负向速度减至 `-2.5 cm/s` 时，从制动提前切换到正向反制动。
- 将反制动范围提高到 `+60~80 pulse`，尽早抵消正向回滚动量。
- 恢复 KEY1 最大时长为 `5000 ms`，符合任务时限。
- 保持 `0 -> +5 cm` 控制参数不变。

## 验证

- Keil 全量编译：`0 Error(s), 0 Warning(s)`。
