# 保持位置误差放宽至 0.7cm

- 代码提交：本文件所在提交
- 日期：2026-07-31

## 修改内容

- `TASK_HOLD` 的位置允许误差由 `+/-0.5 cm` 调整为 `+/-0.7 cm`。
- 回中心末段的固定起步补偿在误差进入 `+/-0.7 cm` 后不再强制施加，减少为追求最后 `0.2 cm` 而产生的反复微调。
- 速度限制 `+/-0.5 cm/s`、至少 `800 ms`、至少 `12` 帧连续有效视觉观测的保持验证保持不变。

## 涉及文件

- `Core/Src/main.c`

## 验证

- 使用 Keil ARMCC 完整构建工程，结果见本次提交。
