# 收紧保持前的平衡验证

- 代码提交：本文件所在提交
- 日期：2026-07-31

## 日志结论

- 日志在 `X=-0.5cm`、速度 `0.6cm/s` 时进入 `TASK,HOLD`。
- 保持后钢球仍带有惯性，继续漂移至约 `-2.3cm`；说明原先 `0.8cm / 2.0cm/s / 300ms` 的验证不足以确认实际平衡。

## 修改内容

- 进入保持状态前，收紧为位置误差不超过 `0.5cm`、速度不超过 `0.5cm/s` 并连续维持 `800ms`。
- 保持后的行为不变：不重新进入闭环调整，电机仍保持当前倾角与使能状态。
- 进入保持时清除 `MICRO` 状态，避免串口继续显示已停止的微调状态。

## 涉及文件

- `Core/Src/main.c`

## 验证

- 使用 Keil ARMCC 完整构建工程，结果见本次提交。
