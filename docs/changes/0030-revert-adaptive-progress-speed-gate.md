# 撤销推进速度门限自适应补偿

- 代码提交：本文件所在提交
- 日期：2026-07-31

## 需求

- 撤销 `0025-adaptive-progress-speed-gate.md` 对静摩擦自适应补偿的修改。

## 修改内容

- 触发条件恢复为：误差在当前 `+/-0.8cm` 微调区外，且绝对速度不高于 `1.5cm/s`。
- 初始补偿、最大补偿和更新周期恢复为 `15` 脉冲、`150` 脉冲和 `250ms`。
- 补偿释放恢复为每周期减 `5` 脉冲，不再使用推进速度比例判定和 10 脉冲快速释放。
- `0026` 的 `+/-0.8cm` 微调区、终端捕获制动、任务状态机和电机参数均保持不变。

## 涉及文件

- `Core/Src/main.c`

## 验证

- 使用 Keil ARMCC 完整构建工程，结果见本次提交。
