# 0074 - 机械水平零点改为 -7.5 度

## 变更

- 将 `MOTOR_FIXED_ZERO_COUNTS` 从 `-1875` 改为 `-1365`。
- 软件的 `PULSE=0` 和 `TILT_REF=0` 重新对应 ZDT 驱动器显示 `-7.5` 度的机械水平位置。

## 换算

电机一圈位置反馈为 65536 counts：

```text
-7.5 / 360 x 65536 = -1365.3 counts
```

取整后使用 `-1365` counts。
