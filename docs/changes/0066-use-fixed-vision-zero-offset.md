# 0066 - 使用固定视觉零点偏置

日期：2026-08-01

## 修改

- 移除 0065 的 `KEY3 + KEY4` 运行时视觉零点校正。
- 视觉 X 坐标对所有帧直接使用固定偏置 `-10.2 cm`：`校正X = 原始X + 10.2 cm`。
- 钢球在机械中心 O 时，原始 `B,-102,y` 自动成为控制坐标 `X=0.0 cm`。

## 影响

- 无需任何按键操作即可测试任务。
- 四个按键全部恢复各自原有功能。
- 电机机械零点和所有闭环参数未修改。
