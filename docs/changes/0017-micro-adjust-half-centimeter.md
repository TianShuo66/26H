# 2cm 微调区与 0.5cm 终点误差

- 代码提交：本文件所在提交（提交说明：`Use micro adjustment near target`）
- 日期：2026-07-31

## 修改内容

- 位置死区和任务完成位置误差从 `+/-1.0 cm` 收紧到 `+/-0.5 cm`。
- 钢球进入目标 `+/-2.0 cm` 后进入微调区，摆杆目标限制为 `+/-24` 脉冲，不再输出大倾角。
- 微调区内停止累加静态偏置，避免补偿在目标附近变大。
- 串口新增 `MICRO,0/1`；`MICRO,1` 表示当前正在 `2 cm` 微调区内。

## 涉及文件

- `Core/Src/main.c`
- `Core/Src/usart.c`
- `Core/Inc/usart.h`

## 验证

- 待本提交后执行 Keil 构建验证。
