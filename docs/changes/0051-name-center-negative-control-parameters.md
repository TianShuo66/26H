# 0051 - 为中心点与负五厘米参数命名

日期：2026-08-01

## 修改内容

- 将中心点参数声明为 `TASK_CENTER_*`。
- 将 `-5 cm` 参数声明为 `TASK_NEGATIVE_*`。
- 两组参数仍分别装载到各自的运行配置中，数值及运行逻辑均未改变。

## 调参位置

- 中心点：`Core/Src/main.c` 中的“中心点闭环参数”区域。
- `-5 cm`：`Core/Src/main.c` 中的“`-5cm` 闭环参数”区域。
- `+5 cm` 继续使用原有参数区域，不受本次修改影响。

## 回退

回退此变更可执行：

```powershell
git revert <0051-commit>
```
