# 0096 - 统一负向目标为 -6 cm

## 修改

- 核对当前运行源码：KEY1 的 `0 -> +5 -> -6 cm` 自动任务、KEY4 直接负向任务和负向保持判断均读取 `TASK_NEGATIVE_TARGET_DECI_CM = -60`。
- 将两处仍写为 `-5 cm` 的负向保持注释更正为 `-6 cm`。
- 不修改历史变更记录，历史记录必须保留当时的 `-5 cm` 参数与行为，才能用于回退。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过。
