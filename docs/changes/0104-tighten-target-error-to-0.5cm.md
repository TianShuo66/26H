# 0104 - 收紧目标误差至 0.5 cm

## 修改

- 通用任务到点判定从 `+/-0.8 cm` 收紧为 `+/-0.5 cm`。
- KEY1 负向终点 `-6 cm` 的连续保持判定从 `+/-0.7 cm` 收紧为 `+/-0.5 cm`。
- 视觉位置死区本来就是 `+/-0.5 cm`，保持不变。

## 影响范围

- 不修改 `0 -> +5 cm` 的推进和制动参数。
- 仅收紧“到达/保持目标”的允许位置误差。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过，`0 Error(s), 0 Warning(s)`。
