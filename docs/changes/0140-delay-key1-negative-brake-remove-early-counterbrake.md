# 0140 - 延后 KEY1 负向制动并取消提前反制动

## 修改原因

两次实测均显示，提前反制动在 `X=-4.6 cm`、仍有负向速度时开始，将钢球强制停在约 `-3.5 cm`，无法进入终点误差范围。

## 修改内容

- 将负向终点制动触发区从距目标 `1.0 cm` 缩小为 `0.5 cm`。
- 移除提前正向反制动，保留向负方向运动时的单向制动。
- 当钢球速度反向或归零时，重新交给现有低速闭环控制。
- `0 -> +5 cm` 段及 `5 s` 任务上限保持不变。

## 验证

- Keil 全量编译：`0 Error(s), 0 Warning(s)`。
