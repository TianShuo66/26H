# 0118 - 控制行为恢复至 1a6b2be

## 修改

- KEY1 控制行为恢复为提交 `1a6b2be` 的版本。
- 恢复负向 `-6 cm` 目标、负向曲线补偿、独立的高速末段捕获反制动及其快速电机跟随步长。
- 恢复正向阶段 `+4.5 cm` 反转阈值和原有起步自适应倾角。

## 保留项

- 本地工作区的 `5000 ms` 任务超时、`700 ms` 反向助推及标定注释不纳入此提交。

## 验证

- Keil `F407_STEP2_EmmV5` 全量编译通过，`0 Error(s), 0 Warning(s)`。
