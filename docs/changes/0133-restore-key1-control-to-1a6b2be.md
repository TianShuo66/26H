# 0133 - KEY1 控制恢复至 1a6b2be

## 修改

- KEY1 恢复 `0 -> +5 cm -> -6 cm`，并在钢球达到 `+4.5 cm` 时折返。
- 恢复负向曲线补偿、`45~90 pulse` 的独立高速终点制动，以及 `40 pulse` 的制动指令步长。
- 移除后续引入的负向提前捕获、制动提前释放和回弹重推策略。

## 保留

- 任务启动时从电机反馈同步实际倾角，避免把未水平的摆杆误判为零位。
- KEY2、KEY3、KEY4、机械平衡调试和视觉接口保持当前实现。
